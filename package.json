{
  "name": "pyflow-backend",
  "private": true,
  "scripts": {
    "build": "cd backend && pip install --no-cache-dir --break-system-packages .",
    "start": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 3001",
    "test": "cd backend && python3 -m pytest"
  }
}
